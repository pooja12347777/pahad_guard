services:
  ner-landslide-ai:
    build: .
    container_name: ner-landslide-ai
    ports:
      - "8501:8501"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./models:/app/models